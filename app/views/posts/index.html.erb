<h1>個人参加募集一覧</h1>

<%# if (@posts.user.due_date || @posts.user.capacity != @posts.due_date || @posts.posts.capacity) %>
  <ul>
    <% @posts.each do |post| %>
      <li><%= post.sports %>&emsp;<%= link_to post.title, post %></li>
      <div><%= post.erea %>&emsp;レベル：<%= post.level %></div>
      <div>開催日：<%= post.event_date.strftime("%Y年 %m月 %d日") %>
      &emsp;時間帯：<%= post.open.strftime("%H時:%M分") %>
      ～<%= post.closed.strftime("%H時:%M分") %></div>
    <% end %>
  </ul>
<%# end %>

<%= link_to '新規募集の投稿', new_post_path %>